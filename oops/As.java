package oops;


public  class As {
// private can be used only in same class

// default can be acccessd in same class+same package subclass +same package non sub class but not in different package

//protected can be  used in : same class+subclasses+same package+subclass but not in a different package non-subclass

//public can be used everywhere
int marks=7;
}
